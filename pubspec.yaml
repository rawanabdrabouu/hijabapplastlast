name: hijaby_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you are publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"  # Updated to ensure compatibility with super-parameters feature

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.31.1
  firebase_database: ^10.5.6
  uuid: ^4.4.0
  awesome_notifications: ^0.9.3+1 

  flutter_svg: ^2.0.10+1
  file_picker: ^8.0.3
  firebase_storage: ^11.7.6
  http: ^1.2.1
  image_cropper: ^6.0.0
  flutter_spinkit: ^5.1.0
  firebase_messaging: ^14.9.3
  flutter_local_notifications: ^17.1.2
  universal_io: ^2.0.4
  url_launcher : ^6.2.6
  image_picker: ^1.1.1 # Replace with the latest version
  intl: ^0.19.0
  flutter_native_splash: ^2.3.2
  get: ^4.3.8
  iconsax: ^0.0.8
  cupertino_icons: ^1.0.2
  provider: ^6.1.2
  material: ^1.0.0+2
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  shared_preferences_android: ^2.2.2
  firebase_analytics: ^10.10.6
  cloud_functions: ^4.7.5
  google_sign_in: ^6.2.1
  googleapis_auth: ^1.6.0
  googleapis: ^13.1.0
  firebase_auth: ^4.19.6
  flutter_speed_dial: ^7.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

 
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.webp" # path to your logo file
  adaptive_icon_background: "#ffffff" # background color for Android adaptive icon
  adaptive_icon_foreground: "assets/images/logo.webp" # path to foreground image for Android adaptive icon
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo.webp
    - assets/icons/

#hi